import React from 'react'
import { Container } from './ApplicationMadeElements'

const ApplicationMade = ({children}) => {
  return (
    <Container>
        {children}
    </Container>
  )
}

export default ApplicationMade