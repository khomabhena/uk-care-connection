import React from 'react'
import { Container } from './ApplicationDetailsElements'

const ApplicationsDetails = ({children}) => {
  return (
    <Container>
      {children}
    </Container>
  )
}

export default ApplicationsDetails